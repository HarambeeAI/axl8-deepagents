# Deep Agents Backend Dependencies

# Core LangGraph and LangChain
langgraph>=0.2.50
langchain>=0.3.0
langchain-core>=0.3.0
langchain-anthropic>=0.3.0

# Deep Agents (local package)
-e ../libs/deepagents

# Database - for LangGraph checkpointing with Supabase Postgres
asyncpg>=0.29.0
psycopg[binary,pool]>=3.1.0
langgraph-checkpoint-postgres>=2.0.0

# Supabase client
supabase>=2.0.0

# Web server
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
pydantic>=2.0.0

# Environment
python-dotenv>=1.0.0

# Utilities
httpx>=0.27.0

# Sandbox backends (optional - auto-detected)
modal>=0.64.0  # Modal cloud sandbox for production isolation
